const defaults = {
	label: 'Dialog',
	description: '',
	focus: '',
	alert: false,
	onInit: (modal, main) => {},
	onOpen: (modal, main) => {},
	onClose: (modal, main) => {},
};

export default defaults;
