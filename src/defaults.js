const defaults = {
	label: 'Dialog',
	description: '',
	focus: '',
	alert: false,
	onInit: (dialog, main) => {},
	onOpen: (dialog, main) => {},
	onClose: (dialog, main) => {},
};

export default defaults;
